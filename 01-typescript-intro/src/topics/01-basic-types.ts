// escribir todo el cod
// como lucen las variables 
// autodocumentacion 

//Constante, es una variable que sepas que no va a cambiar nunca
const name: string ='Strider';

//variable 
let hpPoint: number | 'FULL'= 95;

const isAlive: boolean = true; 

hpPoint = 'FULL';


console.log(
   { name, hpPoint, isAlive}
);




export{} //exportamos un objeto, le estamos diciendo que este archivo es un modulo