export interface Country { //aca vamos a tomar solo la info que necesitamos de postman
  cca2: string;
  flag: string;
  flagSvg: string;
  name: string;
  capital: string;
  population: number;
  region: string;
  subRegion: string;
}
